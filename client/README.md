# Client

Next.js + Tailwind frontend for the notification delivery system.

## Run

1. Copy env example:

```bash
cp .env.local.example .env.local
```

2. Install dependencies:

```bash
npm install
```

3. Start the app:

```bash
npm run dev
```

The frontend expects the Go API to be running on `http://localhost:8080` by default.
